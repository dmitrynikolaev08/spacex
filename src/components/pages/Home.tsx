import React from 'react'
import { Launches } from '../../context/Launches';

const Home: React.FC = () => {
    return (
        <div>
            <Launches />
        </div>
    );
}

export default Home;